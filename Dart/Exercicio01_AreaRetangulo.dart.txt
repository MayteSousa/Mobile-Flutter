//1. Entrar via teclado com a base e a altura de um retângulo, calcular e exibir sua área.
void main() {
  double base = 10;
  double altura = 5;
  double area = base + altura;
  
  print('A área do triângulo de base $base e altura $altura = $area');
 
}